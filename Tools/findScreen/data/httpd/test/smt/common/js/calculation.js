function Calculation(){
    b = $(window);
    bw = b.width();

    w_d = (18 * bw) / 640;
    w_e = (23 * bw) / 640;
    w_l = (27 * bw) / 640;
    w_c = (38 * bw) / 640;
    w_f = (46 * bw) / 640;
    w_o = (48 * bw) / 640;
    w_m = (60 * bw) / 640;
    w_h = (77 * bw) / 640;
    w_j = (83 * bw) / 640;
    w_p = (178 * bw) / 640;
    w_a = (270 * bw) / 640;
    w_n = (422 * bw) / 640;
    w_k = (501 * bw) / 640;
    w_g = (546 * bw) / 640;
    w_b = (580 * bw) / 640;
    w_i = (584 * bw) / 640;

    w_ii = (581 * bw) / 640;
    
    w_q = (71 * bw) / 640;
    w_r = (86 * bw) / 640;

    h_a = (87 * w_i) / 584;
}